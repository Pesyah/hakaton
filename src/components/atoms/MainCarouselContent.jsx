import React from 'react';

const MainCarouselContent = (props) => {
    return (
        <div className="main-carousel-content">
            <h3>{props.text}</h3>
        </div>
    );
};

export default MainCarouselContent;
